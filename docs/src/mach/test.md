# test

```
usage: mach test [-h] [-a ALGORITHMS]

Test HACL*


optional arguments:
  -h, --help            show this help message and exit
  -a ALGORITHMS, --algorithms ALGORITHMS
                        The algorithms to test.
```
